import { NewNgClassDirective } from './new-ng-class.directive';

describe('NewNgClassDirective', () => {
  it('should create an instance', () => {
    const directive = new NewNgClassDirective();
    expect(directive).toBeTruthy();
  });
});
